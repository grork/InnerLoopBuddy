{
  "name": "codevoid.inner-loop-buddy.test",
  "version": "0.0.1",
  "description": "Sample Workspace for Inner Loop Buddy Tests",
  "scripts": {
    "build": "copyfiles index.html output && copyfiles test.json output",
    "serve": "browser-sync start --server output --watch --no-ui --listen localhost --no-online --no-open --reload-debounce 250"
  },
  "author": "Dominic Hopton",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.29.1",
    "copyfiles": "^2.4.1"
  }
}